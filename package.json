{
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build:prover": "cd packages/rapidsnark && npm install && git submodule init && git submodule update && npx task createFieldSources && npx task buildProver"
  }
}